{
  "name": "@classifyre/e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "e2e:test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "test:remote": "BASE_URL=$BASE_URL API_BASE_URL=$API_BASE_URL NONEXISTENT_VAR=1 playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^26.1.1"
  }
}
